[project]
name = "claude-library-mcp"
version = "0.1.0"
description = "Claude knowledge library MCP server"
requires-python = ">=3.11"
dependencies = ["mcp[cli]>=1.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
